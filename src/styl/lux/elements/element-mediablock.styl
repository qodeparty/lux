/* /////////////////////////////////////////////////////////////////////////// */

.media,
.media-item,
.media-list > li
  display flex
  flex 1 1 auto
  max-width 100%
  align-items stretch


.media > img,
.media > figure,
.media-item > img,
.media-item > figure,
.media-list > li > img,
.media-list > li > figure
[class*='media-']{$cl_attr_first}
  flex 0 0 auto
  order -1

[class*='media-']{$cl_attr_last}
  flex 0 0 auto
  order 9999

.media-elem
.media-group
  display block

// .media-list
//   border-left 1px solid #3399FF

.media-item,
.media-list > li:not(:last-child)
  border-bottom 1px dotted #333


//.media-elem > .media-list

/* /////////////////////////////////////////////////////////////////////////// */
//more template-y

.media-list .media-image
  width 70px
  height 70px

.media-list .media-image:after
  top 30px
  left 18px

.media-image{
  width:100px;
  height:100px;
  border:1px solid #dae1e7;
  background:#f8fafc;
  position:relative;
  border-radius:2px;
}

.media-image:after{
  position:absolute;
  content:attr(data-meta);
  font-size:12px;
  top:43px;
  left:25px;
  color:inherit; /* #dae1e7 */
  text-transform:capitalize;
}

.dot {
  height: 25px;
  width: 25px;
  background-color: #0a0a0a33;
  border-radius: 50%;
  display: inline-block;
  color:#fff;
  position:relative;
  font-weight:bold;
  cursor:pointer;
}

.dot:after{
  content:'×';
  position:absolute;
  top:5px;
  left:7px;
}
